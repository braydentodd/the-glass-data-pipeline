# name: Daily Data Update

# on:
#   schedule:
#     # Run daily at 5 AM EST (10 AM UTC)
#     - cron: '0 10 * * *'
#   workflow_dispatch:
#     inputs:
#       reason:
#         description: 'Reason for manual run'
#         required: false
#         default: 'Manual trigger'
#       backfill_start_year:
#         description: 'Backfill start year (optional, e.g. 2020)'
#         required: false
#         default: ''
#       backfill_end_year:
#         description: 'Backfill end year (optional, e.g. 2024)'
#         required: false
#         default: ''

# jobs:
#   etl:
#     name: Daily ETL
#     runs-on: [self-hosted, Linux, X64]
#     timeout-minutes: 600  # 10-hour hard ceiling; prevents runaway jobs
    
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4
    
#     - name: Set up Python environment
#       run: |
#         python3 -m venv venv
#         source venv/bin/activate
#         pip install --upgrade pip
#         pip install -r requirements.txt
    
#     - name: Run daily ETL with auto-restart
#       continue-on-error: true  # Always run Sync step even if ETL hits max restarts
#       env:
#         DATABASE_URL: ${{ secrets.DATABASE_URL }}
#         BACKFILL_START_YEAR: ${{ github.event.inputs.backfill_start_year }}
#         BACKFILL_END_YEAR: ${{ github.event.inputs.backfill_end_year }}
#         PYTHONUNBUFFERED: "1"
#       run: |
#         source venv/bin/activate
#         chmod +x scripts/run_etl.sh
#         scripts/run_etl.sh nba --max-restarts 999
    
#     - name: Publish to Google Sheets
#       env:
#         DATABASE_URL: ${{ secrets.DATABASE_URL }}
#         GOOGLE_SPREADSHEET_ID: ${{ secrets.GOOGLE_SPREADSHEET_ID }}
#         PYTHONUNBUFFERED: "1"
#       run: |
#         source venv/bin/activate
#         chmod +x scripts/run_publish.sh
#         scripts/run_publish.sh nba

#     - name: Upload logs
#       if: always()
#       uses: actions/upload-artifact@v4
#       with:
#         name: etl-logs-${{ github.run_number }}
#         path: |
#           *.log
#         retention-days: 7
