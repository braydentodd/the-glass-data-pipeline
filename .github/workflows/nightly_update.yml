name: Nightly Data Update

on:
  schedule:
    # Run daily at 5 AM EST (10 AM UTC)
    - cron: '0 10 * * *'
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual run'
        required: false
        default: 'Manual trigger'
      backfill_start_year:
        description: 'Backfill start year (optional, e.g. 2020)'
        required: false
        default: ''
      backfill_end_year:
        description: 'Backfill end year (optional, e.g. 2024)'
        required: false
        default: ''

jobs:
  # Job 1: Update database with latest NBA data
  etl:
    name: ETL - Update Database
    runs-on: [self-hosted, Linux, X64]
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up Python environment
      run: |
        python3 -m venv venv
        source venv/bin/activate
        pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run nightly ETL
      env:
        DB_HOST: ${{ secrets.DB_HOST }}
        DB_NAME: ${{ secrets.DB_NAME }}
        DB_USER: ${{ secrets.DB_USER }}
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        BACKFILL_START_YEAR: ${{ github.event.inputs.backfill_start_year }}
        BACKFILL_END_YEAR: ${{ github.event.inputs.backfill_end_year }}
        PYTHONUNBUFFERED: "1"
      run: |
        source venv/bin/activate
        python3 -u -m src.etl
    
    - name: Upload logs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: etl-logs-${{ github.run_number }}
        path: |
          *.log
        retention-days: 7

  # Job 2: Sync database to Google Sheets (depends on ETL completion)
  sync:
    name: Sync - Database to Sheets
    runs-on: [self-hosted, Linux, X64]
    needs: etl
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python environment
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Create Google credentials file
        run: |
          echo '${{ secrets.GOOGLE_CREDENTIALS_JSON }}' > google-credentials.json
      
      - name: Sync to Google Sheets
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          GOOGLE_CREDENTIALS_FILE: google-credentials.json
          GOOGLE_SPREADSHEET_ID: ${{ secrets.GOOGLE_SPREADSHEET_ID }}
          PYTHONUNBUFFERED: "1"
        run: |
          source venv/bin/activate
          python3 -u -m src.sheets_sync
      
      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sync-logs-${{ github.run_number }}
          path: |
            *.log
          retention-days: 7
